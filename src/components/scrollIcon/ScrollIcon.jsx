import Icon from "../../assets/animated/scroll.png";
import { ScrollDiv, ScrollIconDiv } from "./style";
function ScrollIcon() {
  return (
    <ScrollDiv>
      <ScrollIconDiv src={Icon} />
    </ScrollDiv>
  );
}

export default ScrollIcon;
